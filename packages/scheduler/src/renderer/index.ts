/**
 * @fafawork/scheduler — renderer components.
 *
 * Re-exports UI components for task management.
 * Components depend on: react-router-dom, lucide-react, and
 * window.electronAPI.scheduler* methods.
 */

export { TasksPage } from './renderer/TasksPage';
