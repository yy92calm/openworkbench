# OS
.DS_Store
*.swp
*.swo
*~
Thumbs.db

# Editors
.vscode/
.idea/

# Node
node_modules/
dist/
dist-ssr/
*.local
.vite/
.turbo/
*.tsbuildinfo
coverage/

# Rust / Tauri
target/
apps/desktop/src-tauri/target/
apps/desktop/src-tauri/gen/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.uv/

# Logs & caches
*.log
.cache/
.eslintcache

# Local runtime & secrets (never commit)
.workbench/
secrets/
*.key
.env
.env.*

# Build artifacts / installers
*.dmg
*.app
*.exe
*.msi

# Bundled sidecar binaries (fetched per-platform; too large for git)
apps/desktop/src-tauri/binaries/
apps/desktop/binaries/

# App config (contains API keys, provider config)
app-config/

# Electron build output
apps/desktop/release/
apps/desktop/out/

# Stray hashed build artifacts from one-off vite builds (e.g. index-*.js, xlsx-*.css)
apps/desktop/*-*.js
apps/desktop/*-*.css

# ZCode tooling (local plans, session state)
.zcode/

# Local-only deployment plan (contains server addresses, tokens, SSH paths)
DEPLOYMENT.local.md

